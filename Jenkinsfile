pipeline {

    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Build started'
            }
        }
        stage('show files') {
            steps {
                sh 'ls'
            }
        }
    }
}
