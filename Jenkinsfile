pipeline {

    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Build started'
            }
        }
        stage('Build') {
            steps {
                sh 'ls'
            }
        }
    }
}
