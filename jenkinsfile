pipeline {

    agent any

    tools {
        jdk 'JDK17'
    }

    stages {

        stage('Build') {
            steps {
                   echo 'Build started'
//                 sh './gradlew clean build'
            }
        }

    }

    post {
        success {
            echo 'Build Successful'
        }

        failure {
            echo 'Build was Failed'
        }
    }
}